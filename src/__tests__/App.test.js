// import React from 'react';
// import * as deps from './test-util/test-util';
// import App from '../containers/App';
// import { retrieveItems, changeCategory } from '../actions/actions';
// import { OBJECT_DATA } from '../constants/constants';

describe('App tests', () => {
  it('Makes sure prints the components Filter and Clothe', () => {
    // const renderer = new deps.ShallowRenderer();
    // renderer.render(<App
    //   categories={deps.CATEGORIES}
    //   picturesDirectory={deps.PICTURES_DIRECTORY}
    //   retrieveItems={retrieveItems}
    //   changeCategory={changeCategory}
    //   objectData={OBJECT_DATA}
    // />);
    // const result = renderer.getRenderOutput();
    // console.log(result);

    // expect(result.type).toBe('div');
  });
});
